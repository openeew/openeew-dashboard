scalar DateTime
scalar Latitude
scalar Longitude
scalar UUID

type Sensor {
  id: ID!
  activated: DateTime
  lastCheckin: DateTime
  firmwareVer: String
  latitude: Latitude
  longitude: Longitude
  uuid: UUID
  status: String
}

type Query {
  sensors: [Sensor]
}

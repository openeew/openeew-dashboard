datasource db {
  provider = "sqlite" 
  url = "***"
}

generator client {
  provider = "prisma-client-js"
}

model Sensor {
  id          Int      @id @default(autoincrement())
  createdAt   DateTime @default(now())
}
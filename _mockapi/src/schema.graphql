scalar DateTime

type Query {
  allSensors: [Sensor!]!
  accStream: [AccelerationPacket]
}

type Sensor {
  id: ID!
}

type AccelerationPacket {
  x: [Float]
  y: [Float]
  z: [Float]
  device_t: DateTime
}